Contains two environments:
- Sepsis
- TutorBot

The code that creates a behavior policy and sample trajectories:

For `sepsis`, it's `sepsis/create_behavior_policy.py`.

For `tutorbot`, it's inside the main method of `tutor_env.py`.